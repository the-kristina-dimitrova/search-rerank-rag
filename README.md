# Study Notes

Drop your PDF lecture notes and papers here.

Then run:
```bash
python -m scripts.build_notes
```

That's it. All PDFs in this folder are chunked by section headings
and combined into a single searchable corpus.

To add new material later, drop more PDFs here and re-run the command.
